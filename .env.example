# === AI Assistant Configurations ===
# The API key for whichever cloud LLM provider is active (OpenAI, Gemini, Anthropic, OpenRouter)
PROVIDER_API_KEY=

# The API key for whichever cloud voice service is active (ElevenLabs, Gemini STT/TTS)
VOICE_API_KEY=

# === Database ===
DATABASE_URL=sqlite:///data/db/assistant.db




ya pero si el hud no esta activado o visible osea serrado y le pido cambiar a modo camara obviamente lo tien que activar el hud y cambiar a modo camara eso si no esta activo el hud me entiendes a lo que me refiero? porque no debe de dar error es una funcion que existe y si el hud no esta disponible abrirlo y cambiarlo para el modo screen no porque ese si debe de funcionar asi no esta abierto el hud tanto para activar y desactivar pero para el de camara si se nececita el hud si o si que este abierto.